apiVersion: apps/v1
kind: Deployment
metadata:
  name: app2
  namespace: hardened
  labels:
    app: app2
    demo: hardened
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app2
  template:
    metadata:
      labels:
        app: app2
        demo: hardened
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "app2"
        dapr.io/app-protocol: "http"
        dapr.io/app-port: "8081"
        dapr.io/config: "app2-config"
        dapr.io/log-as-json: "true"
        dapr.io/log-level: "debug"
    spec:
      containers:
      - name: app
        image: ghcr.io/mchmarny/hardened-app2:v0.1.2
        ports:
        - containerPort: 8081
        env:
        - name: PUBSUB_NAME
          value: "pubsub"
        - name: TOPIC_NAME
          value: "messages"
        - name: STORE_NAME
          value: "state"
---
apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: app2-config
  namespace: hardened
spec:
  tracing:
    samplingRate: "1"
  secrets:
    scopes:
      - storeName: kubernetes
        defaultAccess: allow
        deniedSecrets: ["redis-secret"]
  accessControl:
    defaultAction: deny
    trustDomain: "public"
    policies:
    - appId: app1
      defaultAction: deny 
      trustDomain: "public"
      namespace: "hardened"
      operations:
      - name: /ping
        httpVerb: ["POST"] 
        action: allow
          